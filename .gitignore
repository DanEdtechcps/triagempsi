# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment variables
.env
.env.*
!.env.example
!.env.production

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

test-results/
playwright-report/

# Mídia gerada (vídeos/áudios)
*.mp4
*.aac
temp_audio/

# Documentação gerada (auditorias, achados, guias de onboarding, specs) —
# fica em documentação viva/ só como referência local, nunca vai pro GitHub.
documentação viva/AUDITORIA_*.md
documentação viva/PENDENCIAS_CLINICAS_*.md
documentação viva/ACHADOS_*.md
documentação viva/VALIDACAO_*.md
documentação viva/ONBOARDING_*.md
documentação viva/ROADMAP_*.md
documentação viva/PESQUISA_*.md

# Runner externo da Fase 2 de geração de psicoeducação (NotebookLM) — ainda
# não implementado; a credencial usada por ele nunca pode ir pro Git.
content-pipeline/.env
documentação viva/specs/
openspec/
